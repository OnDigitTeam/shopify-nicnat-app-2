{
  "name": "nicnat-shopify-shipping",
  "version": "1.0.0",
  "description": "Minimal Shopify shipping app — always returns two static Nicnat rates.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
